# Commit Policy

## Rule

**NEVER commit changes unless the user explicitly asks for it.**

- Do not auto-commit after completing tasks
- Do not suggest committing unless asked
- Wait for explicit instructions like "commit this" or "commit the changes"
- This applies to all git operations that modify history (commit, amend, merge, etc.)
