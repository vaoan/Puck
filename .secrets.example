# Template for the gitignored `.secrets` file used by deployed environments.
# These map onto the secret-bearing keys in `.env.example`. Keep real values
# only in your secret manager / CI secrets, never in git.
SUPABASE_SERVICE_ROLE_KEY=
INTERNAL_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
RESEND_API_KEY=
SMTP_URL=
